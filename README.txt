## Sample app that uses jetty websockets

* clone repo
* verify java is installed, java -v
* verify mvn is installed and using the same version of java, mvn -v
* build, mvn clean install
* run jetty, mvn jetty:run
* open browser to http://localhost:8080/websocket.jsp
